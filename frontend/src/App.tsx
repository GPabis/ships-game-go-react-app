import { PrimaryButton, SecoundaryButton } from './components/Atoms';

function App() {
    return (
        <div>
            <PrimaryButton secoundaryColor>Hello</PrimaryButton>
            <SecoundaryButton>Hoho</SecoundaryButton>
        </div>
    );
}

export default App;
